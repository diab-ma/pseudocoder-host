package config

// DefaultAddr is the default listen address for the WebSocket server.
const DefaultAddr = "127.0.0.1:7070"

// DefaultRepo falls back to the current working directory.
const DefaultRepo = "."
